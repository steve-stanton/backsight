This project can be used to display a Shapefile.
It exists mainly to confirm that the MapControl in the Backsight project is working ok.

Note: Polygons are regarded as closed lines as far as selections are concerned (pointing
well inside a polygon will not select it).